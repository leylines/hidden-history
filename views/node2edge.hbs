
  {{>node2edge-table}}

  {{#ifCond req.user.role '==' 'admin'}}
  <button class="btn btn-primary" data-toggle="modal" data-target=".add-form">Add Node-Edge-Relation</button>
  {{/ifCond}}

  {{#ifCond req.user.role '==' 'admin'}}
  {{>node2edge-form mode='add'}}
  {{/ifCond}}

  <script type="text/javascript">
    $(document).ready(function(){
      var url = window.location.href;
      $(".node2edgeTable").DataTable({
        pageLength: 20,
        paging: true,
        dom: 'frtip'
      });
      {{#ifCond req.user.role '==' 'admin'}}
      var table = $(".node2edgeTable").DataTable();
      $('.discard-add').click(function(){
        $('.nodeTypeId-add').prop('selectedIndex',0);
        $('.edgeTypeId-add').prop('selectedIndex',0);
        $('.add-form').modal('hide');
      });
      {{/ifCond}}
    });
  </script>
